export interface ScheduleClassType {
  id: number;
  time: { start: string; end: string };
  day: string;
}

export interface ScheduleType {
  num: number;
}
export interface ScheduleDaysType {
  day: string;
}
